package voogasalad_raptiltswagger;

public class DataExample2 {
	/**
	 * This is defined in api, but the authoring environment would contain a writer to write its necessary data
	 */
	//private Reader myReader;
	/**
	 * The necessary objects to be written to
	 */
	//private List<GameObject> objects;
	/**
	*For read a game that has been built in the game authoring environment
	*GAME_LOCATION will be a predefined string (made upon the choice of where the projects folders should be)
	*/
	//objects = myReader.read(GAME_LOCATION);
	/**
	 * This is handled by the game authoring environment. 
	 */
	//code that uses objects
}
